clc;
clear all;
n=input('enter the inverval n=');
h=input('enter the value h=');
for i= 1: n+1
    f(i)=input('enter the value f(i)=');
end
sum=(f(1)+f(n+1))/2;
for j=2:n
    sum=sum+f(j);
end
integral=h*sum;
integral
